# Build-time environment for BuhoGO.
# Copy to .env.local (untracked) and fill in real values — never commit keys.

# Breez SDK API key. Used only when the Spark engine is switched to 'breez'
# (localStorage buhoGO_spark_engine); the direct Spark engine ignores it.
VITE_BREEZ_API_KEY=
